# OrderFlow POC

In-memory concurrent order fulfilment proof of concept in core Java and Maven (OOP, collections, streams, concurrency).

Implementation is delivered on the `feature/orderflow-implementation` branch.
